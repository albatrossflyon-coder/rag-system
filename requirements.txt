# Core — required for all usage
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Optional: Cohere reranking (local heuristic fallback used when omitted)
cohere>=5.0.0

# Optional: Pinecone cloud vector store (local BM25+TF-IDF used when omitted)
pinecone>=3.0.0

# Optional: Ragas evaluation metrics
ragas>=0.1.1
datasets>=2.15.0
